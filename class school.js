class school{
    constructor(){
        this.student=[];
        this.teacher=[];
    }
    addteacher(){
         addteacher.push(teacher);
        if(!(teacher instance of teacher))
        throw error("teacher should be teacher")

    }
    fireteacher(){
        this teacher.Splice(index)
    }


    rigisterstudent()
} rigisterstudent.push(student){

    if(!( student instance of student))
    throw error("student should be student");
}
firestudent(){
    this student.splice(index);
}
console.log(addteacher,fireteacher);
console.log(rigisterstudent,firestudent)


}